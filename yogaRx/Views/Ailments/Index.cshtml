@model IEnumerable<yogaRx.Models.Ailment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AilmentName)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.AilmentDesc)
        </th>*@
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AilmentName)
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.AilmentDesc)
        </td>*@
        <td>
         
            @Html.ActionLink("Details", "Details", new { id=item.AilmentId }) 
           
        </td>
    </tr>
}

</table>
